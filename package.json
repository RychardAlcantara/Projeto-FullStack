{
    "name": "project-root",
    "version": "1.0.0",
    "scripts": {
        "start": "concurrently \"npm start --prefix app\" \"npm run dev --prefix api\""
    },
    "devDependencies": {
        "concurrently": "^7.6.0"
    }
}
